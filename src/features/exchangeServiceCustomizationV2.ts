const KEY="anp_exchange_service_order_v2";
const label=(e:Element)=>((e as HTMLElement).innerText||e.getAttribute("aria-label")||"").replace(/\s+/g," ").trim();
function install(){const hero=document.querySelector(".exchange-hero");const box=hero?.nextElementSibling as HTMLElement|null;if(!hero||!box||!box.classList.contains("quick-actions")||box.dataset.anpExPersonal)return;const buttons=Array.from(box.querySelectorAll("button,[role=button]")).filter(b=>{const r=(b as HTMLElement).getBoundingClientRect();return r.width>0&&r.height>0}) as HTMLElement[];if(buttons.length<2)return;box.dataset.anpExPersonal="1";let order:string[]=[];try{order=JSON.parse(localStorage.getItem(KEY)||"[]")}catch{}const map=new Map(buttons.map(b=>[label(b),b]));const desired=order.filter(n=>map.has(n));for(const n of buttons.map(label))if(n&&!desired.includes(n))desired.push(n);for(const n of desired){const b=map.get(n);if(b)box.appendChild(b)}const save=()=>localStorage.setItem(KEY,JSON.stringify(Array.from(box.querySelectorAll("button,[role=button]")).map(label).filter(Boolean)));buttons.forEach(el=>{el.style.touchAction="manipulation";let timer=0,down=false,drag=false,moved=false,sx=0,sy=0;const reset=()=>{clearTimeout(timer);down=false;drag=false;moved=false;el.classList.remove("anp-ex-personal-dragging");el.style.touchAction="manipulation"};el.addEventListener("pointerdown",e=>{down=true;sx=e.clientX;sy=e.clientY;timer=setTimeout(()=>{if(down){drag=true;el.classList.add("anp-ex-personal-dragging");el.style.touchAction="none";navigator.vibrate?.(16)}},560)});el.addEventListener("pointermove",e=>{if(Math.hypot(e.clientX-sx,e.clientY-sy)>14)moved=true;if(!drag||!moved)return;e.preventDefault();const target=buttons.find(x=>x!==el&&(()=>{const r=x.getBoundingClientRect();return e.clientX>=r.left&&e.clientX<=r.right&&e.clientY>=r.top&&e.clientY<=r.bottom})());if(target)box.insertBefore(el,target)});el.addEventListener("pointerup",()=>{if(drag)save();reset()});el.addEventListener("pointercancel",reset)});
}
export function installExchangeServiceCustomizationV2(){if((document.body as any).dataset.anpExPersonalInstalled)return;(document.body as any).dataset.anpExPersonalInstalled="1";const s=document.createElement("style");s.textContent=".quick-actions .anp-ex-personal-dragging{outline:2px solid rgba(0,214,176,.65)!important;transform:scale(1.025);opacity:.82!important;box-shadow:0 8px 26px rgba(0,214,176,.18)!important}";document.head.appendChild(s);let queued=false;const run=()=>{if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;install()})};new MutationObserver(run).observe(document.body,{childList:true,subtree:true});setTimeout(run,500);setTimeout(run,1200)}
